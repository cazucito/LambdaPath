package poc.util;

/**
 * Message types for console output formatting.
 *
 * @author cazucito
 */
public enum MessageType {
    HEADER, FOOTER, TITLE, SUBTITLE, MESSAGE, MESSAGE_OK, LOGIC_ERROR, INFO, EXCEPTION, ERROR, DEBUG
}
